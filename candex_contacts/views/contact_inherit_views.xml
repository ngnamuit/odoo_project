<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- Partner Form View-->
	<record id="candex_view_partner_form" model="ir.ui.view">
		<field name="name">candex.res.partner.form</field>
		<field name="model">res.partner</field>
		<field name="priority" eval="1" />
		<field name="arch" type="xml">
			<form string="Partners">
				<sheet>
					<field name="is_company" invisible="1"/>
                    <field name="type" invisible="1"/>
                    <field name="image_1920" widget='image' class="oe_avatar" options='{"preview_image": "image_128"}'/>
					<div class="oe_title">
						<field name="is_company" invisible="1" />
						<field name="company_type" widget="radio" class="oe_edit_only" options="{'horizontal': true}" />
						<h1>
							<field name="name"  attrs="{'invisible':[('name', '=', '')]}" placeholder="Full name" readonly="1"/>
						</h1>
					</div>
					<group>
						<group>
							<field name="title_name" placeholder="Name Title" default_focus="1"/>
							<field name="first_name"  placeholder="First Name" required="1" />
							<field name="last_name"  placeholder="Last and Middle Name" required="1" />
							<field name="email" widget="email" />
							<field name="phone" widget="phone" />
							<field name="function" placeholder="e.g. Sales Director" />
						</group>
						<group>
							<field name="function" placeholder="e.g. Sales Director" />
							<field name="title" placeholder="e.g. Sales Director" />
							<field name="company_name"/>
							<field name="lifecycle_stage"/>
							<field name="last_contacted"/>
						</group>
					</group>
					<notebook colspan="4">
						<page name="internal_notes" string="Internal Notes">
							<field name="comment" placeholder="Internal note..." />
						</page>
					</notebook>
				</sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers"/>
                    <field name="message_ids" widget="mail_thread"/>
                </div>
			</form>
		</field>
	</record>

	<!-- Partner Tree View-->
	<record id="view_candex_partner_tree" model="ir.ui.view">
		<field name="name">res.partner.tree</field>
		<field name="model">res.partner</field>
		<field name="inherit_id" ref="base.view_partner_tree"/>
		<field name="arch" type="xml">
			<xpath expr="//tree" position="replace">
				<tree string="Contacts">
					<field name="title_name" />
					<field name="first_name" />
					<field name="last_name" />
					<field name="phone"/>
					<field name="email"/>
					<field name="function" />
					<field name="title" />
					<field name="company_id" />
					<field name="lifecycle_stage" />
					<field name="last_contacted" />
				</tree>
			</xpath>
		</field>
	</record>
</odoo>